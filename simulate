#!/usr/bin/env bash

[[ -z "$2" ]] && echo "You need at least two arguments for this, lol" && exit

g++ -std=c++11 $1 -o Player1 || exit
g++ -std=c++11 $2 -o Player2 || exit

python3 arena.py "$PWD/Player1" "$PWD/Player2"
